export enum SiteType {
  SITE = 'site',
  THEME = 'theme',
  PAGE = 'page',
}

export enum SiteCategory {
  WEBSITE = 'website',
  LANDING_PAGES = 'landing-pages',
}

export enum SiteSettingTabs {
  GENERAL,
  PUBLISHING,
  SEO,
  FONTS,
  INTEGRATION,
  CUSTOM_CODE,
  MEMBERS,
  SCREENSHOTS,
  BUSINESS_DETAIL,
  SOCIAL_LINKS,
}
